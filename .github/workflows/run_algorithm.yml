name: 🌌 Infinity Scale Unified System

on:
  workflow_dispatch:
    inputs:
      record_count:
        description: '총 생성 데이터 건수'
        required: true
        default: '200000'
  push:
    paths:
      - 'services/**'
      - 'scripts/**'

env:
  PROJECT_NAME: "omni-infinity-system"
  DOCKER_IMAGE: "omni-infinity-server"
  VERSION: "v2026.${{ github.run_number }}"

jobs:
  # [Job 1] 데이터 생산 및 관리 설정 동기화
  factory-and-sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 📦 Install Python Dependencies
        run: pip install pandas numpy pyyaml

      - name: 🤖 Run Dependabot Manager (Self-Correcting)
        run: |
          python - <<EOF
          import os, json, yaml
          from pathlib import Path
          
          # Dependabot 설정 자동 생성
          Path(".github").mkdir(exist_ok=True)
          config = {
              "version": 2,
              "updates": [
                  {"package-ecosystem": "github-actions", "directory": "/", "schedule": {"interval": "daily"}},
                  {"package-ecosystem": "maven", "directory": "/", "schedule": {"interval": "weekly"}}
              ]
          }
          with open(".github/dependabot.yml", "w") as f:
              yaml.dump(config, f, sort_keys=False)
          
          # 실행 리포트 생성
          with open("dependabot_report.json", "w") as f:
              json.dump({"status": "synced", "timestamp": "${{ env.VERSION }}"}, f)
          EOF

      - name: 🏭 고속 분할 데이터 생성기 실행 (os 에러 수정 완료)
        run: |
          mkdir -p data_chunks
          python - <<EOF
          import pandas as pd
          import numpy as np
          import uuid
          import os  # 에러 해결: os 모듈 명시적 임포트
          
          TOTAL = int(os.environ.get('RECORD_COUNT', 200000))
          CHUNK_SIZE = 50000
          
          print(f"🚀 총 {TOTAL}건 데이터 생성 시작...")
          
          for i in range(0, TOTAL, CHUNK_SIZE):
              current_size = min(CHUNK_SIZE, TOTAL - i)
              df = pd.DataFrame({
                  'uuid': [str(uuid.uuid4()) for _ in range(current_size)],
                  'amount': np.random.randint(1000, 1000000, size=current_size),
                  'status': np.random.choice(['ACTIVE', 'PENDING', 'COMPLETED'], size=current_size)
              })
              df.to_csv(f'data_chunks/chunk_{i//CHUNK_SIZE}.csv', index=False)
              print(f"✅ Chunk {i//CHUNK_SIZE} 생성 완료 ({current_size} records)")
          EOF

      - name: 📤 아티팩트 저장
        uses: actions/upload-artifact@v4
        with:
          name: system-assets
          path: |
            data_chunks/
            .github/dependabot.yml
            dependabot_report.json

      - name: 🚀 Commit & Push Management Config
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .github/dependabot.yml dependabot_report.json
          git diff --staged --quiet || (git commit -m "chore: auto-sync system config [${{ env.VERSION }}]" && git push origin main)

  # [Job 2] 시스템 빌드 및 배포
  system-deployment:
    needs: factory-and-sync
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: 📥 자산 다운로드
        uses: actions/download-artifact@v4
        with:
          name: system-assets
          path: assets/

      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: 🏗️ 시스템 아키텍처 재구축 (컴파일 에러 방지 필드 추가)
        run: |
          BASE="src/main/java/com/costdata/filemanagement"
          mkdir -p $BASE/config $BASE/controller $BASE/dto $BASE/model $BASE/repository src/main/resources/data
          
          # 데이터 파일 이동
          cp assets/data_chunks/*.csv src/main/resources/data/

          # [1] pom.xml 구성
          cat <<EOF > pom.xml
          <project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
            <modelVersion>4.0.0</modelVersion>
            <parent><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-parent</artifactId><version>3.2.2</version></parent>
            <groupId>com.costdata</groupId><artifactId>infinity-api</artifactId><version>1.0.0</version>
            <dependencies>
                <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-web</artifactId></dependency>
                <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-security</artifactId></dependency>
                <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-validation</artifactId></dependency>
                <dependency><groupId>org.projectlombok</groupId><artifactId>lombok</artifactId><optional>true</optional></dependency>
            </dependencies>
          </project>
          EOF

          # [2] DTO 수정 (getPath, getContent 에러 해결)
          cat <<EOF > $BASE/dto/FileRequest.java
          package com.costdata.filemanagement.dto;
          import jakarta.validation.constraints.NotBlank;
          import lombok.Data;
          @Data
          public class FileRequest {
              @NotBlank private String fileName;
              @NotBlank private String path;      // getPath() 매핑용
              @NotBlank private String content;   // getContent() 매핑용
          }
          EOF

          # [3] Controller 구현
          cat <<EOF > $BASE/controller/FileManagementController.java
          package com.costdata.filemanagement.controller;
          import com.costdata.filemanagement.dto.FileRequest;
          import jakarta.validation.Valid;
          import org.springframework.web.bind.annotation.*;
          import java.util.Map;
          @RestController @RequestMapping("/api")
          public class FileManagementController {
              @PostMapping("/files")
              public Map<String, String> createFile(@Valid @RequestBody FileRequest request) {
                  return Map.of("status", "success", "processed_path", request.getPath());
              }
          }
          EOF

          # [4] Main App
          cat <<EOF > $BASE/Application.java
          package com.costdata.filemanagement;
          import org.springframework.boot.SpringApplication;
          import org.springframework.boot.autoconfigure.SpringBootApplication;
          @SpringBootApplication public class Application { public static void main(String[] args) { SpringApplication.run(Application.class, args); } }
          EOF

          # [5] Security Config
          cat <<EOF > $BASE/config/SecurityConfig.java
          package com.costdata.filemanagement.config;
          import org.springframework.context.annotation.Bean;
          import org.springframework.context.annotation.Configuration;
          import org.springframework.security.config.annotation.web.builders.HttpSecurity;
          import org.springframework.security.web.SecurityFilterChain;
          @Configuration
          public class SecurityConfig {
              @Bean public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
                  http.csrf(csrf -> csrf.disable()).authorizeHttpRequests(auth -> auth.anyRequest().permitAll());
                  return http.build();
              }
          }
          EOF

      - name: 🔨 Maven 빌드
        run: mvn clean package -DskipTests

      - name: 🐳 Docker 이미지 빌드 및 Release
        run: |
          cat <<EOF > Dockerfile
          FROM eclipse-temurin:17-jdk-alpine
          COPY target/*.jar app.jar
          ENTRYPOINT ["java", "-Xmx2g", "-jar", "app.jar"]
          EOF
          docker build -t ${{ env.DOCKER_IMAGE }}:${{ env.VERSION }} .
          docker save ${{ env.DOCKER_IMAGE }}:${{ env.VERSION }} > system_bundle.tar

      - name: 📦 GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.VERSION }}
          files: |
            target/*.jar
            system_bundle.tar
            dependabot_report.json
