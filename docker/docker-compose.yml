version: '3.8'
services:
  soc-dashboard:
    image: nginx:alpine
    container_name: soc-dashboard-v1
    ports:
      - "8080:80"
    volumes:
      - ../:/usr/share/nginx/html:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    restart: always
