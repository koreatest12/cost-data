apiVersion: apps/v1
kind: Deployment
metadata:
  name: omni-job
spec:
  replicas: 3
  selector:
    matchLabels:
      app: omni-job
  template:
    metadata:
      labels:
        app: omni-job
    spec:
      containers:
      - name: app
        image: ghcr.io/koreatest12/cost-data/omni-job-service:latest
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 30
